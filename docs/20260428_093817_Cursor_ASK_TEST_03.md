# Ask Test 03

- Endpoint: `POST /ask`
- Status: `500`
- Question: `What is the average salary by job_id`

## Response

```json
{"detail":{"error":"db_error","detail":"ORA-00942: table or view \"DUSBRID_TRANS\".\"EMPLOYEES\" does not exist\nHelp: https://docs.oracle.com/error-help/db/ora-00942/","sql":"SELECT job_id, AVG(salary) AS average_salary FROM employees GROUP BY job_id FETCH FIRST 1000 ROWS ONLY;"}}
```
