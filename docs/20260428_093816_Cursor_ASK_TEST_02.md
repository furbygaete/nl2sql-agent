# Ask Test 02

- Endpoint: `POST /ask`
- Status: `500`
- Question: `Show count of employees by department_id`

## Response

```json
{"detail":{"error":"db_error","detail":"ORA-00942: table or view \"DUSBRID_TRANS\".\"EMPLOYEES\" does not exist\nHelp: https://docs.oracle.com/error-help/db/ora-00942/","sql":"SELECT department_id, COUNT(*) AS employee_count FROM employees GROUP BY department_id FETCH FIRST 1000 ROWS ONLY;"}}
```
